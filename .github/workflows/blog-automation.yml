name: 🤖 Auto Blog Generator

on:
  schedule:
    - cron: '0 8 * * *' # Runs daily at 8:00 AM UTC
  workflow_dispatch: # Allow manual trigger

permissions:
  contents: write

jobs:
  generate-blog:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repo
        uses: actions/checkout@v5

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'

      - name: 📝 Generate Blog Post
        env:
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          EMAILJS_SERVICE_ID: ${{ secrets.EMAILJS_SERVICE_ID }}
          EMAILJS_TEMPLATE_ID: ${{ secrets.EMAILJS_TEMPLATE_ID }}
          EMAILJS_PUBLIC_KEY: ${{ secrets.EMAILJS_PUBLIC_KEY }}
        run: node scripts/generate-blog.js

      - name: 📤 Commit and Push
        run: |
          git config user.name "Kartik Gupta (Auto-Blog)"
          git config user.email "guptakartik614@gmail.com"
          git add content/blog/ src/data/blogs.json src/data/subscribers.json
          git diff --cached --quiet || git commit -m "🤖 Auto-generated blog post [$(date '+%Y-%m-%d')]"
          git push
